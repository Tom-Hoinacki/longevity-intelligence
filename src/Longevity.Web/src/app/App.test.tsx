import {render,screen} from '@testing-library/react'; import {MemoryRouter,Routes,Route} from 'react-router-dom'; import {describe,expect,it} from 'vitest'; import {Shell} from '../layout/Shell'; import {Home} from '../pages/Pages';
describe('shell',()=>{it('renders the product shell',()=>{render(<MemoryRouter><Routes><Route element={<Shell/>}><Route path="/" element={<Home/>}/></Route></Routes></MemoryRouter>); expect(screen.getByText(/Longevity Intelligence/)).toBeInTheDocument(); expect(screen.getByText(/Explore Evidence/)).toBeInTheDocument(); ['Dashboard','Assets','Claims','Sources'].forEach(x=>expect(screen.getByText(x)).toBeInTheDocument()); expect(screen.getByText(/not medical advice/)).toBeInTheDocument();});});
